import { TestBed } from '@angular/core/testing';

import { VerifyProService } from './verify-pro.service';

describe('VerifyProService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: VerifyProService = TestBed.get(VerifyProService);
  //   expect(service).toBeTruthy();
  // });
});
